{
  "name": "talon-fx-toolchain",
  "private": true,
  "description": "Local build tools only. Compiles assets/fx.js from fx-src/. Nothing here ships to the browser directly; the site itself stays static.",
  "scripts": {
    "build": "esbuild fx-src/fx.js --bundle --minify --format=iife --global-name=TalonFX --outfile=../assets/fx.js"
  },
  "dependencies": {
    "@paper-design/shaders": "0.0.80",
    "esbuild": "0.28.2"
  }
}
